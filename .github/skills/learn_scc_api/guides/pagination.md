# Pagination

The Security Cloud Control API operations that list a collection also support pagination.

## Organizations

You can set the cursor and max request query parameters for the collection operations.

If you do not set the max query parameter, the system returns up to 100 records per request. The default cursor value is 1.
